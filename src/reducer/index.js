import { PLACEHOLDER } from '../actions'

const initialState = {
    
}

const reducer = (state = initialState, action)=>{
    switch(action.type){
        case PLACEHOLDER:
            return {
                ...state,
                
            }
        case PLACEHOLDER:
            return {
                ...state,
                
            }
        case PLACEHOLDER:
            return{
                ...state,
                
            }

        case PLACEHOLDER:
            return{
                ...state,
                
            }
        case PLACEHOLDER:
            return{
                ...state,
                
            }
        case PLACEHOLDER:
            return{
                ...state,
                
            }
            
        default:
            return state
    }
}

export default reducer